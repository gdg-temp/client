import styled from 'styled-components';

const LinkFormWrapper = styled.div`
  display: flex;
  flex-direction: column;
  gap: 8px;
`;

const LinkTopWrapper = styled.div`
  display: flex;
  align-items: center;
  gap: 8px;
`;

export default { LinkFormWrapper, LinkTopWrapper };
