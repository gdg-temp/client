// recoil과 react-query에서 사용할 키값을 모아둡니다.
const USER = 'USER_STATE';

const CARDS = 'CARDS_STATE';

const CARD = 'CARD_STATE';

const CARD_BODY = 'CARD_BODY_STATE';

const LINK_BODY = 'LINK_BODY_STATE';

const COLLECTION = 'COLLECTION_STATE';

export default { USER, CARDS, CARD, CARD_BODY, LINK_BODY, COLLECTION };
